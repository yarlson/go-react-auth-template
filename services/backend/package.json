{
  "name": "backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "go run main.go",
    "build": "go build -o dist/backend",
    "test": "go test ./...",
    "lint": "golangci-lint run",
    "format": "goimports -w .",
    "docker:build": "docker build -t go-react-auth-template-backend -f Dockerfile ."
  }
}
